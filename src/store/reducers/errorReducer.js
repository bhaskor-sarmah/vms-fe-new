const initialState = {
  driver: {},
  vehicle: {},
  token: {},
};

const errorReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return {};
  }
};

export default errorReducer;
